source "https://rubygems.org"

# Matches the GitHub Pages build environment so `bundle exec jekyll serve`
# locally renders the same output Pages will publish.
gem "github-pages", group: :jekyll_plugins
gem "webrick", "~> 1.8"
